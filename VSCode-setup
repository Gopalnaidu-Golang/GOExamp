
How to open devpod in  VSCode

How to Debug

How to reload the page:
on Mac:  cmd+shift+p

How do install dependencies

VScode shortcut:
cntl+shift+p to reload work page
command+press to go caller Func definition
cntl+ - = to go back page
Debud procedure
Command + forwareslash = to comment block of code

cfgProvider, err := uConfig.NewYAML(uConfig.Static(map[string]map[string]string{
		"macropoint": {"host": "http://127.0.0.1"}}))
